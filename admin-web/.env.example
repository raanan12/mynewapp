# Same project as the app. Only the public anon key - RLS decides what an
# admin account can see/edit, same as everywhere else. Never put the
# service_role key or any Kesher credential here; this site has no business
# touching either.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
